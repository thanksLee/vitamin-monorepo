## 6. 🧰 AI-CBD Development Prompt Kit (V1.0)

---

> **💡 사용법:**
> 1. 다이어그램의 **단계(Node)**를 확인합니다.
> 2. 아래 키트에서 해당 단계의 **프롬프트**를 복사합니다.
> 3. **Phase 1부터 순서대로** 진행해야 가장 효과가 좋습니다.
> 4. `{ }` 괄호 안에 현재 프로젝트의 내용을 채워 넣고 AI에게 던지세요.
> 5. **Phase 4**는 **FE(프론트)**와 **BE(백엔드)**를 나누어서 각각 진행하세요.
> 6. AI의 답변이 마음에 들지 않으면 "더 구체적으로 써줘", "표 형식으로 바꿔줘"라고 피드백하세요.
> 
> 

---

### 🏗️ Phase 1. 기획 및 분석 (Analysis & Planning)

### 🎨 [P1X] 화면 스케치 (UI Draft)

* **목표:** 텍스트 요구사항을 시각적 묘사로 변환하여 구체화.
* **Input:** 요구사항 정의서.

```markdown
# Role: UX 디자이너
# Input: 요구사항 정의서
"{요구사항 텍스트 붙여넣기}"

# Request
위 요구사항을 구현하기 위한 **'화면 구성요소(UI Text Wireframe)'**를 텍스트로 스케치해줘.
그림 파일 대신, 각 화면에 어떤 정보, 버튼, 입력창이 배치되어야 하는지 구체적으로 나열해줘.
(예: [상단] 뒤로가기 버튼, [중앙] 이메일 입력 필드, [하단] 인증 요청 버튼)

```

#### 📌 [P1B] 사용자 스토리 & 인수조건 작성

* **목표:** 모호한 요구사항을 명확한 개발 기준(인수조건)으로 변환.
* **Input:** 날것의 요구사항, 아이디어.

```markdown
# Role: 시니어 기획자 (PO)

# Input
- 요구사항: "{여기에 구현하고 싶은 기능 설명}"

# Request
위 요구사항을 바탕으로 '사용자 스토리(User Story)'를 작성해줘.
   
   - **User Story:** As a(역할), I want to(기능), So that(가치) 형식.
   - **Acceptance Criteria (핵심):** 개발 완료 여부를 판단할 수 있는 구체적인 조건 4가지 이상 (예외 상황, 제약 조건 포함 필수).

다음 형식을 반드시 지켜줘.

1. **Story Card (3C):**
   - As a (누가)
   - I want to (무엇을)
   - So that (왜/가치)

2. **Acceptance Criteria (인수 조건) - 매우 중요:**
   - 개발 완료 여부를 판단할 수 있는 구체적인 조건 4가지 이상.
   - 예외 케이스(실패 조건)와 수치적 제약(시간, 글자 수 등)을 포함할 것.


#### 📌 [P1G] 핵심 용어 정의 (Data Dictionary)

* **목표:** 기획서와 다이어그램에서 명사를 추출하여 영어 변수명 통일.
* **Input:** 사용자 스토리, 시퀀스 다이어그램 내용.

```markdown
# Role: 데이터 아키텍트 (DA)

# Input
- 사용자 스토리 및 시퀀스 흐름: "{위에서 만든 스토리 및 로직 붙여넣기}"

# Request
위 내용을 분석하여 시스템에서 관리해야 할 '핵심 데이터 용어'를 정의해줘.
출력은 아래 마크다운 표 형식으로 해줘.

| 한글명(Biz Term) | 영문명(Logical Name) | 설명 및 규칙 | 비고 |
| :--- | :--- | :--- | :--- |
| 예: 인증번호 | auth_code | 6자리 숫자 | |
| 예: 만료일시 | expired_at | 발송 후 3분 | |

```

---

### 📐 Phase 2. 상세 설계 (Detailed Design)

#### 📌 [P2A] 유스케이스 상세 시나리오 작성

* **목표:** 인수조건과 용어를 결합하여, 시스템 관점의 상세 로직 기술.
* **Input:** 시퀀스 다이어그램 흐름, 용어 사전, 인수조건.

```markdown
# Role: 테크니컬 라이터 (Technical Writer)

# Context
- 유스케이스명: "{유스케이스 이름}"
- 핵심 용어: "{P1G에서 만든 용어 사전}"
- 로직 흐름: "{시퀀스 다이어그램의 흐름 요약}"

# Request
위 정보를 조합하여 **'유스케이스 상세 시나리오'**를 작성해줘.

**[작성 규칙]**
1. 주어는 명확히 구분할 것 ('사용자' vs '시스템').
2. 문장 작성 시 **핵심 용어(영문명)**를 괄호 안에 병기할 것. (예: 시스템은 `auth_code`를 생성한다.)
3. 기본 흐름(Basic Flow), 대체 흐름(Alt Flow), 예외 흐름(Exception Flow)으로 목차를 나눌 것.
4. **인수 조건**에 있던 제약 사항(시간, 횟수 등)을 로직에 반드시 녹여낼 것.

```

#### 📌 [P2B & P2C] Task 분할 및 단위 테스트 정의

* **목표:** 상세 시나리오를 개발자가 수행할 작업 티켓으로 변환.
* **Input:** P2A에서 만든 상세 시나리오.

```markdown
# Role: 스크럼 마스터 (Scrum Master)

# Input: P2A 상세 시나리오
"{P2A 시나리오 전체 붙여넣기}"

# Request
개발자가 수행할 **Role별 Task 리스트**와 **단위 테스트(DoD)**를 정의해줘.
가독성을 위해 **요약 표**와 **상세 명세**를 나누어서 작성해줘.

**1. Task 요약 표 (Summary Table)**
- **Task ID:** (예: T-BE-01)
- **Role:** BE / FE / DE
- **Task 명:** 작업의 제목
- **선행 작업:** 의존성 (Task ID로 표기)

**2. Task별 단위 테스트 상세 (Details)**
- 표 아래에 각 Task ID별로 **'완료 조건(Unit Test Cases)'**을 구체적인 체크리스트 형태로 나열해줘.
- 각 체크리스트는 수치, 에러 코드, 상태 값 등을 포함해야 함.

**[필수 조건]**
BE Task의 첫 번째는 반드시 '테이블 설계 및 표준화'여야 함.

```
---

#### 📌 [P2D] 통합 테스트 시나리오

* **목표:** 전체 흐름을 검증할 통합 테스트 시나리오 작성.
* **Input:** P2A에서 만든 상세 시나리오.

```markdown
# Role: QA 엔지니어
# Input: P2A 상세 시나리오
"{상세 시나리오 붙여넣기}"

# Request
전체 흐름을 검증할 **'통합 테스트 시나리오(Integration TC)'**를 작성해줘.
- 개발자가 아닌 **사용자 관점**의 E2E 테스트여야 함.
- 포함: TC ID, 테스트 상황, 사전 조건, 단계(Step), 기대 결과, 테스트 데이터.
- 정상 흐름과 주요 예외 흐름(Timeout 등)을 모두 포함할 것.

```

---

### 🛠️ Phase 4. 기술 구현 설계 (Implementation)

#### 📌 [P4A & P4B] API 및 DB 명세서 생성

* **목표:** Task를 수행하기 위한 기술적 설계도 자동 생성.
* **Input:** 상세 시나리오, 용어 사전.

```markdown
# Role: 수석 백엔드 개발자

# Input
- 상세 시나리오: "{P2A 시나리오}"
- 용어 사전: "{P1G 용어}"

# Request 1: API 명세
위 기능을 구현하기 위한 REST API 명세서를 **OpenAPI 3.0 (YAML)** 형식으로 작성해줘.
- Request/Response Body 예시 포함.
- 성공(200) 및 실패(400, 401, 500) 케이스에 대한 에러 코드 정의 포함.
- Request/Response 예시와 에러 코드 4xx, 5xx 포함 필수

# Request 2: DB 설계
위 기능을 지원하기 위한 **Postgresql DDL(테이블 생성 쿼리)**을 작성해줘.
- 테이블명과 컬럼명은 '용어 사전'의 영문명을 따를 것.
- 각 컬럼에 Comment(주석)를 달 것.

```

---

### 📦 [P4D] 컴포넌트 설계 (Module Definition)

* **목표:** 기능의 덩어리(모듈) 나누기. **(Top-Down 시작)**
* **Input:** P2A 시나리오.

```markdown
# Role: 소프트웨어 아키텍트
# Input: P2A 시나리오
"{내용 붙여넣기}"

# Request
이 시스템을 구성할 **'핵심 컴포넌트(모듈)'** 구조를 설계해줘.
- 각 컴포넌트의 역할(Responsibility)과 포함 범위(Boundary)를 정의해줘.
- (예: 회원 컴포넌트, 인증 컴포넌트, 알림 컴포넌트)

```

---

### 🔌 [P4F] 인터페이스 정의서 (I/O & Contract)

* **목표:** 컴포넌트의 입출력 규격 정의.
* **Input:** P4D 컴포넌트 구조.

```markdown
# Role: 백엔드/프론트엔드 리드 (선택)
# Input: P4D 컴포넌트 구조
"{내용 붙여넣기}"

# Request
각 컴포넌트가 제공해야 할 **'인터페이스(Interface/Type)'**를 정의해줘.
- 내부 구현 로직은 제외하고, **메서드 시그니처(함수명, 파라미터, 리턴타입)**만 정의할 것.
- 입력값과 출력값의 데이터 구조(DTO/Type)를 명확히 할 것.

```

---

### 💻 [P4E] 클래스 설계 (Internal Logic)

* **목표:** 실제 동작하는 내부 로직 설계.
* **Input:** P4F 인터페이스 정의서.

```markdown
# Role: 시니어 개발자
# Input: P4F 인터페이스 정의서
"{내용 붙여넣기}"

# Request
위 인터페이스를 실제로 구현할 **'클래스 설계(Class Design)'**를 해줘.
- Controller - Service - Repository 패턴(또는 해당 프레임워크 패턴)을 따를 것.
- 각 클래스의 주요 메서드 내부 로직(Pseudo-code)을 간략히 기술해줘.

```

---

### ⚖️ Phase 5. 검증 및 피드백 (Verification)

#### 📌 [P5A] 추적성 검증 (Story vs Task)

* **목표:** 요구사항 누락 방지.
* **Input:** 사용자 스토리(인수조건), Task 리스트.

```markdown
# Role: QA Auditor (감사관)

# Input
- A (인수조건): "{P1B 인수조건}"
- B (Task 리스트): "{P2B Task 표}"

# Request
A(요구사항)가 B(작업목록)에 빠짐없이 반영되었는지 **추적성 검사**를 수행해줘.
만약 Task로 구현되지 않고 **누락된 인수조건**이 있다면 정확히 지적해줘.

```

---

#### 📌 [P5B] 적합성 검증 (Task vs Unit TC)

* **목표:** 테스트 케이스가 Task에 맞는지 검증.
* **Input:** Task 리스트 및 단위 테스트 상세 명세.

```markdown

# Role: QA 리드 (Test QA)

# Input: Task 리스트 및 단위 테스트 상세 명세 (P2B+P2C 결과물)
"{전체 내용 붙여넣기}"

# Request
작성된 **Task 리스트**와 **단위 테스트 상세 내용** 간의 **'적합성 검증'**을 수행해줘.

**[체크리스트]**
1. **매칭 정확성:** 각 Task의 성격에 맞는 테스트 케이스가 연결되었는가? (DB설계 Task에 API 테스트가 붙어있지 않은가?)
2. **구체성:** 테스트 완료 조건이 '확인한다' 같이 모호하지 않고, **수치(6자리)나 에러코드(404)** 등으로 명확한가?
3. **누락 여부:** 모든 Task에 대해 테스트 케이스가 존재하는가?

문제가 있는 항목이 있다면 **Task ID**와 함께 지적해줘.

```

---

#### 📌 [P5C] 정합성 검증 (Scenario vs DB Spec)

* **목표:** 논리적 설계와 물리적 설계의 불일치(데이터 구멍) 탐지.
* **Input:** 상세 시나리오, 테이블 명세서(DDL).

```markdown
# Role: DB 모델러

# Input
- A (시나리오): "{P2A 상세 시나리오}"
- B (테이블 명세): "{P4B DDL 쿼리}"

# Request
**[정합성 체크]**를 수행해줘.
시나리오(A)의 흐름을 처리하기 위해 필요한 데이터가 테이블(B)에 모두 컬럼으로 존재하는가?

**[경고해야 할 상황]**
1. 시나리오에선 저장/조회한다고 했는데 테이블에 컬럼이 없는 경우.
2. 데이터 타입(길이, 숫자/문자)이 시나리오의 로직과 맞지 않는 경우.

``` 

#### 📌 [P5D] 커버리지 검증 (Scenario vs Integrated TC)

* **목표:** 테스트 시나리오가 상세 시나리오의 모든 흐름을 커버하는지 검증.
* **Input:** 상세 시나리오, 통합 테스트 시나리오.

```markdown
# Role: QA 리더
# Input: A(상세 시나리오), B(통합 테스트 시나리오)
"{A, B 붙여넣기}"

# Request
테스트 시나리오(B)가 상세 시나리오(A)의 **모든 흐름(대체/예외 포함)을 커버하는지** 검증해줘.
- 테스트되지 않고 누락된 시나리오 흐름이 있다면 지적해줘.

```
